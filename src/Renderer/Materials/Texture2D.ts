import vertexSource from './Shaders/Texture2DVertex.glsl';
import fragmentSource from './Shaders/Texture2DFrag.glsl';

export const texture2DVertexSource = vertexSource;
export const texture2DFragmentSource = fragmentSource;

